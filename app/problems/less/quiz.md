# Quiz

http://localhost:5173/problem/less
http://localhost:5173/solve/less/1
http://localhost:5173/solve/less/2
http://localhost:5173/solve/less/3

테마를 토글할 수 있는 컴포넌트가 있다. 이 컴포넌트는 현재 테마를 기반으로 배경색과 글자색 정보를 담은 객체를 만들고, 부모로부터 테마가 적용됐을 때 호출할 콜백 함수를 prop으로 받는다. 내부의 이펙트는 그 객체를 DOM에 적용하고, 콜백을 호출해 부모에게 알린다. 그런데 이 이펙트의 의존성 배열은 비어 있고, 린트 경고가 발생하고 있다.

---

**Q1.** deps 배열이 React에서 어떤 역할을 하는지 설명하고, 그 역할을 바탕으로 현재 코드에서 경고가 발생하는 이유를 연결해서 서술하시오.

<br>
<br>
<br>

---

**Q2.** 경고를 없애려고 누락된 값들을 deps에 추가했더니 화면이 멈추지 않고 계속 깜빡인다. 왜 그런지 서술하십쇼.

<br>
<br>
<br>

---

**Q3.** 객체와 콜백 함수, 둘 다 deps에 넣으면 무한 루프가 생긴다. 증상은 같지만 원인이 다르다. 각각 왜 매 렌더마다 새 참조가 만들어지는지 설명하시오.

<br>
<br>
<br>

---

**Q4.** 원인이 다르다면 해결 방향도 달라야 한다. 객체와 콜백 각각에 대해 어떤 방향으로 접근할 수 있는지, 구체적인 방법보다 방향을 먼저 서술하시오.

<br>
<br>
<br>

---

**Q5.** 콜백을 deps에서 제외하면서도 항상 최신 값을 읽으려면 어떻게 해야 하는가? React가 이 패턴을 공식 API로 만들려는 시도와 연결해서 설명하시오.

<br>
<br>
<br>
